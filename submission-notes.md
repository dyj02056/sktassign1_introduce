# 제출 점검 및 수정 기록

## 구현 요약

- 단일 HTML/CSS/JavaScript로 구현한 반응형 랜딩 페이지입니다.
- Hero의 세 CTA는 실행, 기록, 예방 섹션으로 순서대로 이동합니다.
- GitHub 및 Notion 근거 링크는 모두 새 탭에서 선언된 주소로 열립니다.

## 실제 점검 결함과 수정

1. **수정 전:** 고정 폭 레이아웃은 1366px 이하 화면에서 카드가 화면 밖으로 나갈 수 있었습니다.  
   **수정 후:** `width: min(1180px, calc(100% - 40px))`, 유동형 타이포그래피, 680px 미디어 쿼리를 적용해 가로 넘침 없이 한 열로 재배치했습니다.
2. **수정 전:** 스크롤 진입 애니메이션이 사용자 설정과 무관하게 작동해 큰 움직임에 민감한 사용자가 끌 수 없었습니다.  
   **수정 후:** `prefers-reduced-motion`을 적용하고, 마우스·키보드로 사용할 수 있는 “모션 줄이기” 버튼을 추가했습니다.
3. **수정 전:** 장식형 버튼과 링크는 키보드 초점 위치가 충분히 드러나지 않을 수 있었습니다.  
   **수정 후:** 네이티브 `<a>`·`<button>`을 사용하고 고대비 `:focus-visible` 외곽선을 추가했으며, 본문 건너뛰기 링크를 넣었습니다.
4. **수정 전:** 외부 링크가 현재 페이지를 벗어나게 하거나 보안 컨텍스트를 공유할 가능성이 있었습니다.  
   **수정 후:** 모든 근거 링크에 `target="_blank"` 및 `rel="noopener noreferrer"`를 적용했습니다.

## 확인 항목

- 제목 구조: `h1 → h2 → h3` 순서를 지켰습니다.
- 상호작용: CTA 앵커 이동과 모션 제어는 마우스와 키보드 모두로 작동합니다.
- 색 대비: 본문은 밝은 텍스트(`#eef0ed`)와 어두운 철판 배경을 사용하고, 주요 텍스트·CTA는 고대비 색상을 사용했습니다.
- 민감정보: 페이지와 제출물에 비밀번호, 토큰, API 키를 포함하지 않았습니다.
